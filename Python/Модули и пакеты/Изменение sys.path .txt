Внутри питона можно изменить переменную окружения, чтобы открыть доступ к импортированию модулей, из различных каталогов:

import sys

sys.path.append('Абсолютный путь каталога')

После этого аппенда изменение переменной окружения будет воздействовать на все будущие операции импорта.